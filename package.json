{
  "name": "bulk-openai-api",
  "version": "2.0.0",
  "description": "The bulk-openai-api package makes bulk requests to OpenAI API and storing the responses in a SQLite database. The handler also provides functionality to export the stored data as a CSV file.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prepublish": "tsc"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.4",
    "@types/node": "^18.16.3",
    "@types/papaparse": "^5.3.7",
    "typescript": "^5.0.4"
  },
  "dependencies": {
    "better-sqlite3": "^8.3.0",
    "openai": "^3.2.1",
    "papaparse": "^5.4.1"
  }
}
